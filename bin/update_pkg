#!/bin/sh

test -f /etc/debian_version && ( sudo aptitude update ; sudo aptitude -q -y dist-upgrade ; sudo rm -fv /var/tmp/pending_pkg.$( date +'%Y-%m-%d' ) )
test -f /etc/redhat-release && ( sudo yum -y update )
